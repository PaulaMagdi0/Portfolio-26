# Public site URL — used by SEO metadata, sitemap, og-image absolute URLs.
# Production: set to the canonical https://... URL.
# Dev: defaults to http://localhost:3000 if unset.
NEXT_PUBLIC_SITE_URL=https://paulamagdy.com
